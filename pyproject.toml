[project]
name = "lamb-evaluaitor"
version = "0.1.0"
description = "Utilities for evaluating LAMB projects"
requires-python = ">=3.11"
dependencies = [
    "pillow>=12.0.0",
    "python-dotenv>=1.1.1",
    "requests>=2.32.5",
    "rich>=13.0.0",
]

[tool.uv]
# uv specific configuration can go here
